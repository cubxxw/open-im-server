interface:
  display_name: "Open Im Server"
  short_description: "Repo-specific patterns and workflows for open-im-server"
  default_prompt: "Use the open-im-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true